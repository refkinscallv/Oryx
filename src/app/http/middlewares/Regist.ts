import { Express } from 'express';

/** register global middleware for express right here */
export default class HttpMiddlewares {
    public static register(express: Express) {
        // express.use()
    }
}
